# Environment Variables Example
# Copy this file to .env.local and fill in your values

# API Configuration (currently using MSW mock API, no real API needed)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Authentication (currently using mock JWT, no real auth needed)
# JWT_SECRET=your-secret-key-here

# Database (currently using mock-data.json, no real database needed)
# DATABASE_URL=your-database-url-here

# Third-party Services (add when integrating real services)
# STRIPE_SECRET_KEY=your-stripe-key
# SENDGRID_API_KEY=your-sendgrid-key
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret

# Note: This project uses Mock Service Worker (MSW) for API mocking
# No environment variables are required for the basic setup to work
